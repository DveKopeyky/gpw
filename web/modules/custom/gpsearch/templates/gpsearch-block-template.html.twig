<input type="text" class="form-control" name="q" placeholder="{{ 'Search in Global Pact Website'|t }}" value="{{ q }}" {% if is_front %} autofocus {% endif %}>
<span class="glyphicon glyphicon-search form-control-feedback" aria-hidden="true"></span>

