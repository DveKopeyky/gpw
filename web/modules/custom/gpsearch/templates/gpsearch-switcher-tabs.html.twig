{% if (routes) %}<ul class="nav nav-tabs search-switcher-tabs">
  {% for item in routes %}
    <li class="nav-item">
      <a class="nav-link{% if item.active %} active{% endif %}" href="{{ item.url }}">{{ item.label }}</a>
    </li>
  {% endfor %}
</ul>
{% endif %}
